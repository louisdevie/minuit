//use std::fs::File;
//use std::path::Path;

fn main() {
    /*let path = Path::new("src/bindata/test.rs");

    let mut file = match File::create(&path) {
        Err(why) => panic!("Can't create src/bindata/test.rs : {}", why),
        Ok(file) => file,
    };*/
}
